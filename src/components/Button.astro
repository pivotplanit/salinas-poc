---
import { getRelativeLocaleUrl } from "astro:i18n";

interface Props {
  text?: string;
  href?: string;
  variant?: "primary" | "secondary" | "outline";
  size?: "sm" | "md" | "lg";
  fullWidth?: boolean;
  disabled?: boolean;
  target?: string;
  type?: "button" | "submit" | "reset";
  ariaLabel?: string;
  class?: string;
  id?: string;
}

const {
  text,
  href,
  variant = "primary",
  size = "md",
  fullWidth = false,
  disabled = false,
  target,
  type = "button",
  ariaLabel,
  class: additionalClass = "",
  id,
} = Astro.props;

const isLink = !!href;
const currentLocale = Astro.currentLocale || "en";
const localizedHref = href ? getRelativeLocaleUrl(currentLocale, href) : undefined;
const buttonClass =
  `btn btn-${variant} btn-${size} ${fullWidth ? "btn-full-width" : ""} ${additionalClass}`.trim();
---

{isLink ? (
  <a {id} href={localizedHref} class={buttonClass} aria-label={ariaLabel} target={target}>
    <slot>{text}</slot>
  </a>
) : (
  <button {id} class={buttonClass} type={type} disabled={disabled} aria-label={ariaLabel}>
    <slot>{text}</slot>
  </button>
)}

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    border: 1px solid transparent;
    border-radius: 4px;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    transition: all 300ms ease;
    white-space: nowrap;
    outline: none;
    user-select: none;
    text-align: center;
    vertical-align: middle;
  }

  .btn:focus-visible {
    outline: 2px solid #4CAF50;
    outline-offset: 2px;
  }

  /* Sizes */
  .btn-sm {
    padding: 0.5rem 1rem;
    font-size: 0.75rem;
  }

  .btn-md {
    padding: 0.75rem 1.5rem;
    font-size: 1rem;
  }

  .btn-lg {
    padding: 1rem 2rem;
    font-size: 1.125rem;
  }

  /* Variants */
  .btn-primary {
    background-color: var(--primary);
    color: white;
  }

  .btn-primary:hover:not(:disabled) {
    background-color: var(--secondary);
    /* box-shadow: 0 4px 12px rgba(45, 80, 22, 0.2); */
    color: var(--primary);
    border: 1px solid var(--primary);
  }

  .btn-primary:active:not(:disabled) {
    transform: translateY(0);
  }

  .btn-secondary {
    background-color: #8B7355;
    color: white;
  }

  .btn-secondary:hover:not(:disabled) {
    background-color: #6f5444;
    box-shadow: 0 4px 12px rgba(139, 115, 85, 0.2);
  }

  .btn-secondary:active:not(:disabled) {
    transform: translateY(0);
  }

  .btn-outline {
    background-color: transparent;
    color: var(--primary);
    border-color: var(--border-color);
  }

  .btn-outline:hover:not(:disabled) {
    background-color: var(--primary);
    color: white;
    border-color: var(--primary);
    /* box-shadow: 0 4px 12px rgba(45, 80, 22, 0.2); */
  }

  .btn-outline:active:not(:disabled) {
    transform: translateY(0);
  }

  /* Full width */
  .btn-full-width {
    width: 100%;
  }

  /* Disabled state */
  .btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none !important;
  }

  @media (max-width: 768px) {
    .btn-md {
      padding: 0.625rem 1.25rem;
      font-size: 0.95rem;
    }

    .btn-lg {
      padding: 0.875rem 1.5rem;
      font-size: 1rem;
    }
  }
</style>
